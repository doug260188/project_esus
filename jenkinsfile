pipeline {
    agent any

    stages {
        stage('Run Container') {
            steps {
                sh 'export PATH=$PATH:/usr/bin'
                sh 'docker-compose up -d https://raw.githubusercontent.com/doug260188/project_esus/master/docker-compose.yml'
            }
        }
    }
}