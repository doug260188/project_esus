version: '2'
services:
  pec-app:
    image: doug260188/ubuntu:latest
    restart: always
    volumes:
      - ./:/pec-docker/
    ports:
      - 8000:8081
    stdin_open: true
    tty: true
    entrypoint:
      - bash
      - "entrypoint.sh"
    working_dir: /pec-docker/
    mem_limit: 2g